[project]
name = "polyalpha"
version = "0.1.0"
description = "Python Polymarket SDK"
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = [
    "setuptools",
    "wheel"
]

build-backend = "setuptools.build_meta"